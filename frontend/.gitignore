# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
# NOTE: /out/ (the static export) is intentionally COMMITTED so a plain Python
# host can serve the prebuilt site without a Node build step. Rebuild with:
#   STATIC_EXPORT=true NEXT_PUBLIC_API_BASE= NEXT_PUBLIC_SITE_URL=<your-url> npm run build

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
